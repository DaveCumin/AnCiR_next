// @ts-nocheck
import { describe, it, expect } from 'vitest';
import SessionButtons from './SessionButtons.svelte';

describe('SessionButtons', () => {
	it('exports a component', () => {
		expect(SessionButtons).toBeDefined();
	});
});
