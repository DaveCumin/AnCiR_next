// @ts-nocheck
import { describe, it, expect } from 'vitest';
import MiniDataTable from './MiniDataTable.svelte';

describe('MiniDataTable', () => {
	it('exports a component', () => {
		expect(MiniDataTable).toBeDefined();
	});
});
