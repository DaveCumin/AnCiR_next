# Python deps for the AnCiR tools (parity harness, runtime, converter).
# Create an isolated env:  python3.12 -m venv tools/.venv && tools/.venv/bin/pip install -r tools/requirements.txt
numpy>=2.0
scipy>=1.18
pandas>=3.0
matplotlib>=3.8
# statsmodels backs the p-adjust and logistic-regression parity fixtures. It was
# installed into tools/.venv but never pinned here, so a fresh env failed to build.
statsmodels>=0.14
pytest>=8
# PyWavelets is the CWT reference: scipy REMOVED signal.cwt/morlet/morlet2/ricker,
# so there is no scipy fallback for the wavelet parity fixture.
PyWavelets>=1.8
