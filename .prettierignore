# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Build output (tracked so Pages can serve it, but it is a bundler artefact)
build/
static/handbook.html
static/ancir-python-export.js
static/ancir-r-export.js

# Tool caches
.pytest_cache/
tools/.pytest_cache/

# Generated at build time by src/lib/utils/generateBuild.js
src/build-info.js
src/lib/utils/build-info.js

# Generated from the live node registry by scripts/genManifest.mjs
static/nodes.json

# Generated by mcp/src/emit/gen-schema.js (npm run mcp:schema)
mcp/src/emit/session-schema.generated.json

# Generated demo + classroom sessions, rewritten wholesale by the
# generateDemos / generateClassroom / generateTPDemos emitter tests.
static/sessions/

# Golden file rewritten by src/lib/_demos/demoMetrics.test.js
src/lib/_demos/demoMetrics.golden.json

# Benchmark harness: committed result JSON/markdown and generated .cache/.data
# bundles for the paper. Not app source; reformatting would churn the artefacts.
tools/benchmarks/

# Release metadata rewritten by src/lib/utils/generateBuild.js, which emits the
# version with double quotes. Prettier's YAML output prefers single quotes, so
# formatting it would put `pnpm build` and `pnpm lint` in a permanent fight (and
# versionSync.svelte.test.js reads the double-quoted form).
CITATION.cff
